Classic Cloud Command frontend is saved in frontend/src-classic.

To restore it:
  rm -rf frontend/src && cp -a frontend/src-classic frontend/src

Or in the running app: Settings → Appearance → Use classic dashboard.

